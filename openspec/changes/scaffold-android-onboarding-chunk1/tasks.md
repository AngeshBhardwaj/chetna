## 1. Gradle module scaffolding

- [x] 1.1 Verify current stable versions for AGP, Kotlin (already pinned at 2.4.10 — confirm Compose compiler compatibility), Compose BOM, `androidx.navigation-compose`, Hilt, detekt, and JUnit5-on-Android support (AGP native support or the `de.mannodermaus.android-junit5` plugin, to keep the `test` source set on JUnit5/Kotest consistent with the domain modules) via the `verify-dependency-version` skill before pinning anything. Resolved pins recorded in `design.md`'s Open Questions section: AGP 9.1.1 (not 9.3.1 — Kotlin 2.4.10 compatibility ceiling), Compose Compiler plugin 2.4.10, Compose BOM 2026.06.01, navigation-compose 2.9.8, Hilt 2.60.1 via kapt (not KSP), detekt 2.0.0-alpha.6 (user-approved — no stable release supports Kotlin 2.4.10), android-junit5 2.0.1.
- [x] 1.2 Write `apps/gradlew-docker`, a thin wrapper script (`docker run --rm -v <repo>:/workspace -w /workspace/apps -e ANDROID_HOME=/opt/android-sdk-linux <saschpe/android-emulator:verified-tag> ./gradlew "$@"`) per ADR-0015's containerized-build decision (corrected from an earlier host-installed-SDK draft — this machine has no host JDK at all). Every `./gradlew ...` invocation named in the rest of this task list means "via `apps/gradlew-docker`," not a bare host call. While here, bump `apps/gradle/wrapper/gradle-wrapper.properties`'s `networkTimeout`/`retries` — the default 10s timeout was observed failing once against this Docker network path during verification. Install `adb` (platform-tools only) on host, approved as a narrow exception for the physical-device sideload step in task 16 of the parent plan.
- [x] 1.3 Add an Android target to each of `apps/domains/{consent,enforcement,credits,device}`'s `kotlin {}` block, alongside the existing `jvm()`. **Corrected during implementation**: not `com.android.library` + `androidTarget()` as originally written here — AGP 9.0+ requires KMP modules to use the dedicated `com.android.kotlin.multiplatform.library` plugin (`alias(libs.plugins.android.kotlin.multiplatform.library)`) with an `android { namespace; compileSdk; minSdk }` block inside `kotlin {}` (note: an intermediate `androidLibrary {}` block name is deprecated in favor of `android {}` — verified directly via Gradle's own deprecation warning, not assumed from docs, which disagreed with each other on this exact point). `namespace = "in.chetna.domains.<name>"`, `compileSdk = 36`, `minSdk = 26` for all four modules.
- [x] 1.4 Verify `./gradlew :backend:build` still succeeds unchanged from `apps/` after 1.3 — applying the Android KMP plugin to the domain modules puts AGP on `apps/backend`'s classpath transitively; this is not a safe assumption per ADR-0015's Consequences. Verified: `apps/gradlew-docker :backend:build -x test -x integrationTest` succeeds cleanly, no deprecation warnings.
- [x] 1.5 Add `":mobile:android"` to `apps/settings.gradle.kts`.
- [x] 1.6 Create `apps/mobile/android/build.gradle.kts`: `com.android.application`, Compose compiler plugin, Hilt plugin; `implementation(project(":domains:x"))` for all four domain modules; `androidx.navigation-compose`, Hilt, Compose BOM/UI/Material3 dependencies. **Corrected during implementation**: no separate `org.jetbrains.kotlin.android` plugin — AGP 9's built-in Kotlin support handles this natively and errors if it's applied; Hilt uses KSP (`libs.plugins.ksp`), not kapt — kapt is explicitly incompatible with built-in Kotlin (a second, independent reason beyond design.md's original detekt-lag reasoning, which no longer holds since kapt isn't an option here at all); `compileSdk`/`targetSdk` = 37, not 36 — the latest AndroidX libraries (core-ktx 1.19.0, lifecycle 2.11.0) require compiling against API 37. Also added an `androidComponents { onVariants { ... } }` block (casting to `VariantOutputImpl`, the stable public `VariantOutput` interface doesn't expose a settable `outputFileName`) naming the output `chetna-app-<buildType>-<versionName>.apk` (e.g. `chetna-app-debug-0.1.0.apk`) instead of AGP's default `android-debug.apk` — user-requested, so debug/release builds stay distinguishable by filename alone once several are floating around testers' devices.
- [x] 1.7 Add root-level detekt configuration under `apps/`, enforced only on `apps/mobile/android`'s source set (per ADR-0015); run `./gradlew :mobile:android:detekt` against the bare-minimum module and confirm it executes cleanly (zero source files is fine — confirming the task itself runs, not that there's anything to lint yet). Verified: `apps/config/detekt/detekt.yml` generated via `detektGenerateConfig` and wired into the module (`config.setFrom(...)`, `buildUponDefaultConfig = true`); `apps/gradlew-docker :mobile:android:detekt` succeeds cleanly.
- [x] 1.8 Verify `./gradlew :mobile:android:assembleDebug` succeeds with a bare-minimum `MainActivity` (a literal "Hello World" `Text` composable, not the nav shell — that's Section 3) before building anything else on top. Verified: `apps/gradlew-docker :mobile:android:assembleDebug` succeeds, `apps/mobile/android/build/outputs/apk/debug/android-debug.apk` produced.

## 2. Generated Compose theme

- [x] 2.1 Extend `design/tokens/generate-scheme.mjs` (or add a sibling script) to emit `Color.kt` from `design/tokens/generated/color-scheme.json`, with a "do not hand-edit" header matching the existing convention. Added a sibling script (`design/tokens/generate-android-theme.mjs`, `npm run generate:android`) rather than extending `generate-scheme.mjs` directly, keeping the JSON-emitting web pipeline and Kotlin-emitting Android pipeline independently runnable. **Corrected during implementation**: M3's `lightColorScheme()`/`darkColorScheme()` no longer accept a `shadow` parameter (removed from the Compose Material3 API, confirmed via a real compile error) — the generator filters it out of the emitted role list.
- [x] 2.2 Extend the same generation step to emit `Shape.kt` from `design/tokens/tokens.json`'s `shape.radius` values. Emits four named `RoundedCornerShape` vals (`BezelShape`/`CardShape`/`ButtonShape`/`RowShape`) rather than forcing a mapping onto M3's abstract 5-step shape scale, which `tokens.json`'s own component-specific radii don't cleanly map to.
- [x] 2.3 Wire generated output to `apps/mobile/android/src/main/kotlin/in/chetna/mobile/ui/theme/`; add a hand-written `Typography.kt` left at Compose Material 3 defaults (no generation needed, per `tokens.json`'s own "unmodified" note). Also added `Theme.kt` (`ChetnaTheme` composable wrapping `MaterialTheme` with the generated color scheme + typography, light/dark via `isSystemInDarkTheme()`) — needed to actually consume the generated files, not itself generated.
- [x] 2.4 Run the generator and verify the app builds against the generated theme (a minimal screen rendering primary-colored text, removed once real screens exist). Verified: `MainActivity` wraps its "Hello World" `Text` in `ChetnaTheme`, colored `MaterialTheme.colorScheme.primary`; `apps/gradlew-docker :mobile:android:assembleDebug` succeeds.
- [x] 2.5 Verify generation is reproducible and seed-color changes propagate: run the generator twice against an unchanged `tokens.json` and diff `Color.kt`/`Shape.kt` (expect no diff); temporarily change `tokens.json`'s `color.seed`, rerun, and diff again (expect `Color.kt`'s M3 role values to change); revert the temporary change. Verified directly: unchanged rerun produced an empty diff; a temporary seed change (`#4A4076` → `#2E7D32`) changed `primary`/`primaryContainer`/etc. as expected; reverted, confirmed `git status` clean.

## 3. Navigation shell + DI

- [x] 3.1 Write a Compose/`androidTest` test against a stub `NavHost` (stub screens, no real content) asserting: Welcome is the start destination, forward taps transition Welcome → Contact details → OTP verify, system back on Welcome exits the app, and reaching Signed-in leaves the back stack such that a subsequent system-back lands on Welcome (not a popped Contact-details/OTP-verify screen). **Sequencing note**: written directly against the real `NavHost` rather than a red-then-green cycle against a stub — there is no emulator yet (Section 8 hasn't run) to execute a red/green loop against, so "expected to fail" couldn't be observed. Verified `apps/gradlew-docker :mobile:android:compileDebugAndroidTestKotlin` compiles cleanly (catches real API-surface errors even without a device); actual pass/fail execution is deferred to Section 10 once the Dockerized emulator exists.
- [x] 3.2 Implement the `NavHost` with routes for all four screens and the `popUpTo(welcome) { inclusive = false }` back-stack semantics on reaching Signed-in, wired into `MainActivity`, until 3.1 passes. Type-safe routes via `kotlinx.serialization` (`@Serializable sealed interface Route`) — the modern `navigation-compose` 2.9.x pattern (`composable<Route.X>` / `backStackEntry.toRoute<Route.X>()`), required adding the `org.jetbrains.kotlin.plugin.serialization` plugin. `apps/gradlew-docker :mobile:android:assembleDebug` succeeds with the full nav/DI/Hilt graph wired in.
- [x] 3.3 Set up the Hilt `Application` class and base DI module scaffolding (empty modules to extend per-screen, not per-screen bindings yet). `ChetnaApplication` (`@HiltAndroidApp`), registered in `AndroidManifest.xml`; `MainActivity` is `@AndroidEntryPoint`; empty `di/AppModule.kt` (`@InstallIn(SingletonComponent::class)`) to extend later. Also added a `HiltTestRunner` (`androidTest`) and Hilt/espresso/androidx.test dependencies needed for `androidTest` to use `@HiltAndroidTest` — not originally itemized as a separate task, needed to make 3.1 compile at all.

## 4. Welcome screen

- [x] 4.1 Write a Compose UI test (`androidTest`) against a stub Welcome composable, asserting the trust points render and tapping "Get started" navigates away. Same sequencing note as 3.1 — written directly against the real composable (no emulator yet to run red/green), using `createComposeRule()` (not `createAndroidComposeRule`) since `WelcomeScreen` needs no Activity/Hilt. Compiles cleanly; execution deferred to Section 10.
- [x] 4.2 Implement the Welcome composable (icon badge, headline, trust points, full-width "Get started" button calling `navController.navigate()` directly, no back arrow — first screen) per `design.md`'s screen mapping, matching `onboarding-1-welcome.png`'s layout/spacing/alignment and the prototype's content wording, until 4.1 passes. Content pulled verbatim from `design/prototype/src/screens/onboarding/Welcome.tsx`. Added a shared `IconBadge` composable (`ui/components/`) and `androidx.compose.material:material-icons-extended` for the trust-point/badge icons (`Icons.Rounded.VerifiedUser/Block/PrivacyTip/Emergency`). Layout approximates the reference PNG's proportions (icon top, headline/description upper-middle, trust points + button anchored toward the bottom via `Modifier.weight(1f)` spacers) — exact pixel tuning deferred to the Section 10 screenshot-diff task, which exists for exactly this. `apps/gradlew-docker :mobile:android:assembleDebug` succeeds.

## 5. Contact details screen

- [x] 5.1 Write unit tests for the Contact-details ViewModel: email format validation, Continue-enabled state as a function of (valid email × consent checked), SMS option always non-selectable, back-navigation event to Welcome. **Adjusted**: "back-navigation event to Welcome" isn't ViewModel state — matching Welcome's precedent (Section 4), back is a direct `navController.popBackStack()` call from the composable, not a ViewModel-emitted event; nothing to unit-test there. Kotest `FunSpec`, 5 tests, all passing (`apps/gradlew-docker :mobile:android:testDebugUnitTest` — verified via the actual JUnit XML report, not just a green exit code: `tests="5" failures="0" errors="0"`).
- [x] 5.2 Implement the Contact-details ViewModel to satisfy 5.1. Email carried back via `SavedStateHandle`'s individual "email" key (not `toRoute<>()`, which needs internal wiring a plain unit test can't easily construct) — this is what makes the OTP→Contact back-navigation spec requirement (email retained) actually work.
- [x] 5.3 Write a Compose UI test against a stub Contact-details composable, asserting: SMS option cannot be selected; Continue stays disabled until both a valid email and checked consent are present; tapping Continue with valid state navigates to OTP verify carrying the entered email; tapping back navigates to Welcome. Same sequencing note as 4.1 (written against the real composable, execution deferred to Section 10) — constructs `ContactDetailsViewModel` directly (`SavedStateHandle()`, no Hilt) so this stays a plain UI test.
- [x] 5.4 Implement the Contact-details composable (Email/SMS selection rows, email field, consent checkbox, full-width Continue button, back arrow to Welcome) wired to the ViewModel, matching `onboarding-2-phone.png`'s layout/spacing/alignment, until 5.3 passes. Added shared `SelectionCard` and `ScreenShell` composables (`ui/components/`) — content pulled verbatim from `design/prototype/src/screens/onboarding/ContactDetails.tsx`, including the exact email regex. `apps/gradlew-docker :mobile:android:assembleDebug` succeeds.

## 6. OTP verify screen

- [x] 6.1 Write unit tests for the OTP ViewModel/state machine: digit-entry advances focus index, all-6-filled triggers a verify check, correct code transitions to a "verified" state, wrong code decrements attempts and transitions to a "wrong" state, 3rd wrong attempt transitions to a "locked" state, "clear and retry" resets to empty/entering state (only available before lockout), resend does not reset the attempt counter, back-navigation event to Contact details (with the previously entered email retained, per spec). **Adjusted** (same as 5.1): back-navigation is a direct `navController.popBackStack()` call, not ViewModel state — no unit test for it. 7 Kotest tests, virtual-time-tested resend countdown (`StandardTestDispatcher` + `advanceTimeBy`, `@OptIn(ExperimentalCoroutinesApi::class)`). Verified via the actual JUnit XML report: `tests="7" failures="0" errors="0"`.
- [x] 6.2 Implement the OTP ViewModel/state machine to satisfy 6.1. Mock code `123456` matches the prototype exactly. Verified transitions to a one-shot `SharedFlow<Unit>` (`verified`) after an 800ms delay, matching the prototype's `setTimeout(onVerified, 800)` — keeps the ViewModel navigation-agnostic (NavHost owns the actual `navigate()` call).
- [x] 6.3 Write a Compose UI test against a stub OTP composable covering the full wrong-code retry path: enter wrong code → see error + attempts-remaining → tap "Clear and try again" → retry → repeat until locked → verify fields are disabled and no retry action remains, and the locked state's only recovery is the back arrow (no separate in-body button). Same sequencing note as 4.1/5.3. Constructs `OtpViewModel` directly (`SavedStateHandle()`).
- [x] 6.4 Write a Compose UI test against the same stub for the success path (correct code shows verified state and navigates to Signed-in) and for the resend countdown (button disabled during countdown, re-enabled after, does not reset attempts-remaining). **Adjusted**: the UI test asserts only the initial disabled state (`"Resend code in 0:30"`, not enabled) — waiting out a real 30-second countdown in an instrumented test is wasteful; the full countdown-elapses-then-re-enables behavior is already covered by 6.1's virtual-time unit test. Navigation-to-Signed-in on verify is covered by `ChetnaNavHostTest` (Section 3), not duplicated here.
- [x] 6.5 Write a Compose UI test partially entering OTP digits, then triggering a configuration change (`ActivityScenario.recreate()`, simulating rotation) and asserting the entered digits and attempt count are retained rather than reset. Written as `OtpConfigChangeTest` (separate file from 6.3/6.4) — needs a real `MainActivity` + `@HiltAndroidTest`, unlike the other OTP UI tests, since it specifically verifies the Hilt-provided ViewModel survives recreation (the other tests construct the ViewModel directly, bypassing Hilt). Navigates the full Welcome→Contact→OTP path first, per real usage.
- [x] 6.6 Implement the OTP composable (6 boxed digit fields, auto-advance focus, verified/wrong/locked visual states, resend countdown, back arrow to Contact details) wired to the ViewModel, matching `onboarding-3-otp.png`'s layout/spacing/alignment, until 6.3, 6.4, and 6.5 all pass. Content/copy pulled verbatim from `design/prototype/src/screens/onboarding/OtpVerify.tsx` and `shared/OtpInputGroup.tsx`. `apps/gradlew-docker :mobile:android:compileDebugAndroidTestKotlin :mobile:android:assembleDebug` succeeds — clean, no warnings, first attempt.

## 7. Signed-in screen

- [x] 7.1 Write a unit test asserting Logout clears the held email/OTP state and triggers navigation back to Welcome. **Adjusted** (same pattern as 5.1/6.1): navigation is a direct composable-level call, not ViewModel state — tested is the state-clearing half only. Introduced a new `SessionState` (`@Singleton`, `session/` package) not itemized in the original task list: a plain nav argument can't carry the verified email from OTP to Signed-in *and* be clearable on Logout for a clean re-login, so a singleton-scoped holder was needed — `OtpViewModel` writes to it on verification, `SignedInViewModel` reads/clears it. 2 Kotest tests, both passing (verified via JUnit XML).
- [x] 7.2 Write a Compose UI test against a stub Signed-in composable verifying the full round trip: complete the flow to Signed-in, tap Logout, land back on Welcome, re-enter the flow with empty fields (no leftover state from the previous attempt). Needs a real `MainActivity` + `@HiltAndroidTest` (like `OtpConfigChangeTest`) since it exercises `SessionState` end-to-end, not a directly-constructed ViewModel. Execution deferred to Section 10.
- [x] 7.3 Implement the Signed-in composable (icon badge, email display, Logout button, temporary-scaffold caption — no back-arrow in-screen, content matching the prototype's `SignedIn.tsx`, layout/spacing/alignment matching `onboarding-4-signedin.png`) wired to a ViewModel satisfying 7.1, until 7.2 passes. `apps/gradlew-docker :mobile:android:compileDebugAndroidTestKotlin :mobile:android:testDebugUnitTest :mobile:android:assembleDebug` all succeed — the full four-screen flow (Welcome → Contact → OTP → Signed-in → Logout) now builds end-to-end.

## 8. Dockerized emulator (reproducible)

- [x] 8.1 Write `apps/mobile/android/emulator/Dockerfile`, `FROM saschpe/android-emulator:<verified tag>` (verify the tag is still current via `verify-dependency-version`), installing `xvfb x11vnc novnc websockify` at build time. Do **not** bake the AVD into the image (per `design.md`'s decision: create on first container start instead, to keep the image smaller) — the image ships the SDK/`avdmanager`/system-image package only. **Adjusted**: the tag was still current (`35-jdk23.0.2_7`, confirmed on Docker Hub), but the base image ships *no* system images at all (verified — `/opt/android-sdk-linux/system-images` is empty), so the Dockerfile also installs `system-images;android-35;google_apis;x86_64` via `sdkmanager` at build time — static SDK content, not per-machine AVD state, so this doesn't conflict with the "don't bake the AVD" decision; it avoids a multi-GB download blocking every first container start instead. Runs as root throughout (not the base image's default `nonroot`) — matches ADR-0015's noted budtmo precedent, needed for `/dev/kvm` and the AVD volume mount regardless.
- [x] 8.2 Write the container entrypoint script: fix `/dev/kvm` permissions, create the `pixel_9` / API 35 AVD via `avdmanager` if it isn't already present in the mounted AVD volume (first-boot-only cost, per 8.3's volume), then start Xvfb → emulator → x11vnc → websockify in sequence, matching this session's manually-validated command sequence. **Three corrections found only by actually running it** (not in the original manual investigation, which never tested a container *restart*): (1) `adb -a start-server` (bind to all interfaces) added before the AVD step — needed for 8.5. (2) `-read-only` added to the emulator launch flags — a restart against the same persisted AVD volume failed with `FATAL: Running multiple emulators with the same AVD is an experimental feature` (a stale lock from the prior instance's non-graceful shutdown), which the emulator's own error message named `-read-only` as the fix for. (3) A genuine race condition: the emulator was launched right after backgrounding Xvfb with no check that Xvfb was actually ready, so on a host busy right after a Docker engine restart, the emulator's `XOpenDisplay(":1")` call raced ahead and failed permanently (`Fatal: no Qt platform plugin could be initialized`) instead of retrying — reproduced for real, not hypothesized. Fixed by polling `xdpyinfo -display :1` (new `x11-utils` package) for up to 30s before launching the emulator.
- [x] 8.3 Write `apps/mobile/android/emulator/compose.yaml` (standalone, matching the `design/prototype/`/`docs/` pattern) exposing the noVNC port and mounting a named Docker volume for the AVD directory (e.g. `~/.android/avd` inside the container) so the AVD created in 8.2 persists across container restarts instead of being recreated every time; add it to the root `docker-compose.yml`'s `include:` list. Also declares the `chetna-android-build` network used by 8.5.
- [x] 8.4 Verify: `docker compose -f apps/mobile/android/emulator/compose.yaml up --build` boots to a usable emulator reachable at the mapped noVNC port from a clean build (not the manually-committed image from this session's investigation) — first start creates the AVD (slower), and confirm a subsequent `docker compose restart` reuses the persisted AVD volume (faster, no re-creation). Verified directly: clean build → AVD created → device online (`adb devices` → `emulator-5554 device`) → noVNC HTTP 200 at `localhost:6081/vnc.html`; restart → "Reusing existing AVD" logged, no re-creation, no `FATAL` (after the 8.2 fix).
- [x] 8.5 Wire up `connectedAndroidTest` connectivity. **Corrected twice during verification, in the same session**: (1) `adb connect <host>:5555` doesn't work at all — emulator ports 5554/5555 are loopback-only ("Connection refused"). A remote adb *server* does work at the plain CLI level (`adb -a start-server` in the emulator container + `ANDROID_ADB_SERVER_ADDRESS`/`PORT` in a separate build container — verified, `adb devices` sees `emulator-5554`). (2) But AGP's own `connectedAndroidTest` device discovery (`DeviceMonitor`/ddmlib) doesn't honor those env vars — confirmed directly: `[DeviceMonitor]: Cannot reach ADB server`, hanging indefinitely, while a plain `adb` CLI call in the same setup worked fine. Real-world Dockerized Android CI projects (checked directly, not assumed) run the build and the emulator in the *same* container for exactly this reason — no cross-container adb pattern is standard. So `connectedAndroidTest` runs via `docker exec` into the emulator's own container (`emulator/compose.yaml` now also mounts the repo + `chetna-gradle-cache`), where adb is genuinely local. `apps/gradlew-docker` no longer carries the now-unused remote-server logic. Host `adb` (installed per 1.2) stays reserved for the physical-device sideload path.

## 9. CI

- [x] 9.1 Add `.github/workflows/android.yml`, triggered on changes under `apps/domains/**` or `apps/mobile/android/**`, running directly on `ubuntu-latest` (GitHub-hosted runners ship a preinstalled Android SDK — do not use a bare `gradle:*-jdk21` container, which has none and fails `assembleDebug` immediately) — steps: `:mobile:android:testDebugUnitTest`, `:mobile:android:assembleDebug`, and `:backend:build` (confirming the domain modules' new Android target hasn't broken backend's consumption of them). `androidTest` stays local-only per `design.md`'s Non-Goals. Matches `docs.yml`'s shape (path-filtered push + `pull_request` + `workflow_dispatch`). Runs `./gradlew` directly on the runner, not via `apps/gradlew-docker` — this machine's containerization need doesn't apply to `ubuntu-latest`.
- [ ] 9.2 Verify the workflow passes on a real push to the change's branch before considering CI done. **Blocked on user action**: needs an actual `git push` to trigger real GitHub Actions execution — a shared/visible action, not taken without asking. Deferred to when the branch is actually pushed/PR'd.

## 10. Cross-cutting verification

- [x] 10.1 Run the full unit test suite (`:mobile:android:testDebugUnitTest`) and confirm all green. Verified via JUnit XML across all three ViewModel test classes: 14/14 tests passing (Contact 5, SignedIn 2, Otp 7), zero failures/errors.
- [x] 10.2 Run the full instrumented test suite (`:mobile:android:connectedAndroidTest` or equivalent) against the Dockerized emulator from Section 8 and confirm all green. Required the architecture correction in 8.5 (`docker exec` into the emulator container) and a readiness fix: `adb devices` showing "device" is not the same as the system actually being usable — the launcher itself ANR'd on a fresh boot (`sys.boot_completed=1` had fired, but the home screen was still unresponsive); confirmed via a real screenshot, not assumed. Found and fixed 5 real bugs surfaced only by running on an actual device, none caught by unit tests or compilation: (1) `ContactScreen`'s consent row — tapping the label text did nothing, only the small checkbox itself was tappable (`Text` has no click semantics); fixed with `Modifier.toggleable(role = Role.Checkbox)` on the whole row — a real touch-target/accessibility bug, not just a test artifact. (2) `ChetnaNavHostTest` still asserted against Section 3's stub screen text ("Contact details", "Verify $email", "Welcome") never updated after Sections 4-7 replaced the stubs — rewritten against real content. (3) `Espresso.pressBackUnconditionally()` called from inside `onActivity {}` (main thread) — Espresso requires the instrumentation thread; moved out, and switched to checking `scenario.state == DESTROYED` since fetching the activity after it's gone throws. (4) Two `assertIsDisplayed`/`assertExists` calls needed `useUnmergedTree = true` — Compose merged the target text into a parent semantics node (the tool's own error named the fix). (5) `SignedInRoundTripTest` asserted immediately after entering the last OTP digit, without waiting for the ViewModel's real 800ms verify-to-navigate delay; added `waitUntil`. **Two more found on rerun** after 8.x's move to a headless container (below): (6) the same 800ms-wait gap existed in `ChetnaNavHostTest`'s copy of the same assertion — fixed the same way. (7) `Espresso.pressBackUnconditionally()` itself proved flaky (`RootViewWithoutFocusException`, reproduced 3/3 runs, not resource contention — ruled that out directly with the user's own emulator confirmed closed) — replaced with `activityRule.scenario.onActivity { it.onBackPressedDispatcher.onBackPressed() }`, which needed `waitUntil` polling for `scenario.state == DESTROYED` rather than a single check, since Compose idling isn't the same as the Activity lifecycle finishing. Stable across 2 consecutive full runs, 13/13 both times.
- [x] 10.3 Screenshot-verify all four screens via the Dockerized emulator, diffed against `design/prototype/reference/onboarding/welcome-and-auth/screen-exports/`. Found: status bar icons invisible on all screens — `targetSdk` 35+ enforces edge-to-edge, and without `enableEdgeToEdge()`/`isAppearanceLightStatusBars`, icons default to a color that's invisible on our light background (confirmed via Android's edge-to-edge docs, not guessed). Fixed in `MainActivity.kt` and `ui/theme/Theme.kt`. Also found `detekt.yml` was only ever verified against an empty module (task 1.7) — running it against real screens surfaced mostly Compose-unaware false positives (PascalCase `@Composable` naming, generated `Color.kt` literals, long UI functions) plus a few genuine ones (magic numbers, a misnamed file). Config fixed, genuine ones fixed directly. Unit + instrumented tests (13/13) re-verified green.
  - **Second pass, after `enableEdgeToEdge()` above**: content now drew *under* the status bar/cutout/nav bar (no inset padding), and a fidelity diff against the reference found Welcome screen missing its logo, using the wrong icon style (filled, not outline), an oversized badge icon, headline/subtext running full-width instead of wrapping narrower, and buttons using M3's default stadium shape instead of the design system's 20dp radius. Traced the reference's own source (`project-html/`) to confirm which parts were real Chetna design-system output vs. a disclaimed generic device-frame template, rather than eyeballing pixels — colors matched our generated `Color.kt` byte-for-byte. Fixed: `safeDrawingPadding()` on all screens (background painted full-bleed via a `Surface` in `MainActivity`, insets applied to inner content only); logo added as a hand-extracted `res/drawable/chetna_logo_icon_mark.xml` (generation script skipped — SVG→VectorDrawable isn't a build-time step anywhere in this ecosystem); icons swapped `Icons.Rounded.*` → `Icons.Outlined.*` (Material Symbols Rounded, the reference's actual icon set, defaults to outline); `IconBadge` gained an `iconSize` param; `Button` calls given explicit `shape = ButtonShape` and per-variant height/padding. Contact/OTP/SignedIn have their own deferred structural gaps (`TopAppBar` should carry no title, with the headline as a separate block below it; `TextField` should show its label above the box, not as an M3 floating label) — not fixed this round, flagged for a follow-up fidelity pass. Re-verified: build, detekt, unit tests, and instrumented tests (13/13) all green.
  - **Third pass, Contact screen**: back button was merged into the same row as the page title instead of its own isolated app-bar row; the consent checkbox wasn't vertically centered against its label; and the consent+button block sat mid-page instead of pinned to the bottom. Fixed via a `ScreenShell` rewrite (back-only app-bar row + divider, headline as a separate block below it — shared by Contact/OTP) and a `weight(1f)` spacer replacing a fixed one, matching the reference's own `flex:1` slot. Follow-up user audit against the design system's actual source (`_ds_bundle.js`) found and fixed 6 more: consent/subtitle text using `bodyMedium` instead of `bodySmall`, no gap between checkbox and its label, the email field and `SelectionCard` missing the `RowShape`/padding/icon-size tokens used everywhere else. Kept M3's stock `Checkbox`/`OutlinedTextField` (styled via `colors`/`shape`, not replaced) — a deliberate call, not an oversight, to avoid hand-built form components. Re-verified: build, detekt, unit tests, instrumented tests (13/13) all green.
  - **Fourth pass, OTP screen**: digit boxes were 48×48dp with `headlineSmall` (24sp) text — spec is 48×60dp with `titleMedium` (16sp), so the digit was clipping/oversized; boxes had no `shape`/fill/border set at all (M3 defaults, not the design system's rounded `primaryContainer` look); and the resend button sat fixed below the boxes instead of pinned to the bottom. Fixed the same way as Contact (`RowShape`, explicit colors, `weight(1f)` spacer). Extracting the digit row into a private `OtpDigitRow` composable was needed to keep `OtpScreen` under detekt's `LongMethod` threshold, rather than raising the threshold again. Re-verified: build, detekt, unit tests, instrumented tests (13/13) all green.
  - **Fifth pass, Signed-in screen**: content rendered top-left instead of centered — it was wrapped in `ScreenShell`, which always renders a back-button app-bar row (even with no `onBack`) and has no vertical/horizontal centering, but the reference has no app bar on this screen at all, just a fully centered column. Rewrote `SignedInScreen` standalone (no `ScreenShell`), matching the reference's `justify/align: center` layout directly. Badge size bumped from the spec's `96/44dp` to `140/50dp` on request, to match the Welcome screen's badge after its own earlier resize. Re-verified: build, detekt, unit tests, instrumented tests (13/13) all green.
- [x] 10.4 Update `CLAUDE.md`'s `apps/mobile/android` repo-layout row (currently "Not yet scaffolded") to reflect the real module.
- [x] 10.5 Add a "Working on the Android app" section to the root `README.md` (matching the existing "Working on the docs site"/"Working on the design prototype" sections' shape). **Adjusted from the original wording**: no noVNC port to view — the emulator is headless-only (Section 8's pivot) — so the section documents `apps/gradlew-docker` for build/test/detekt, bringing up the emulator for automated `connectedAndroidTest` only, and the one host-side exception (`adb`, for running your own emulator or a physical-device sideload to actually see the app).
- [x] 10.6 Add the missing launcher icon — the app had none at all (`AndroidManifest.xml` had no `android:icon`, no `res/mipmap-*` existed), surfaced only when preparing for physical-device sideload. Built as an Android 8+ adaptive icon (`minSdk` 26 guarantees support, so no legacy density-PNG fallback needed): foreground reuses the exact same 3 fill paths as the in-app Welcome-screen logo mark (via a `<group>` scale/translate transform, not re-derived path data), background matches the `primaryContainer` tone already used behind that same mark in `IconBadge`. `android:icon`/`android:roundIcon` both point at the same adaptive-icon XML — correct for `minSdk` 26+, no separate square/round art needed (the OS masks one source to whatever shape the launcher wants).

## 11. Independent code review

- [x] 11.1 Run the `code-review` skill against `apps/mobile/android` (fresh context, no implementation history). 4 findings, ranked by severity:
  1. **Fixed** — `OtpUiState.isResendEnabled` was gated only on the countdown, not on `OtpStatus.LOCKED`, so Resend rendered as tappable during lockout (spec: back is the only recovery path once locked). Fixed by adding `&& status != OtpStatus.LOCKED`; added a regression test (`OtpViewModelTest`) covering lockout with an elapsed countdown, which is exactly the case that let this through originally.
  2. **Fixed** — `ChetnaTheme` cast `LocalView.current.context` straight to `Activity`, which would crash if ever composed inside a Dialog/popup window (context there isn't the Activity directly). Added a `Context.findActivity()` walk up the `ContextWrapper` chain instead, matching the standard Compose pattern for this.
  3. **Fixed** — domain modules pinned `compileSdk 36`, diverging from the app module's `37` and contradicting `design.md`'s claim they stay in sync.
  4. **Fixed** — all four domain modules duplicated an identical `android {}`/`sourceSets` block verbatim (only `namespace` differed) plus identical dependency declarations. Findings 3 and 4 were fixed together: extracted into a `chetna.kotlin-multiplatform-domain` convention plugin (`apps/build-logic/convention/`, an included build) — each domain module now only sets its own `namespace`; `compileSdk` bumped to 37 in the same pass, resolving the drift. Hit a genuine, still-open Gradle limitation getting there: generated `libs.foo` version-catalog accessors aren't available inside precompiled script plugins (gradle/gradle#15383/#28371, confirmed against Now-in-Android's own convention-plugin source, which works around it the same way) — fixed via `VersionCatalogsExtension.named("libs").findLibrary(...)` instead of the generated accessors.
  Re-verified after all fixes: domain modules + mobile app build/detekt/unit tests green, instrumented tests 13/13 green.
