package `in`.chetna.mobile.ui.theme

import androidx.compose.material3.Typography

// Left at Compose Material 3's own defaults — see tokens.json's typography.note:
// "No custom type scale at v1."
val ChetnaTypography = Typography()
