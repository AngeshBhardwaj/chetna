# docs

Code-adjacent technical artifacts that are naturally versioned with the backend code — not narrative documentation, which lives in the project's top-level `docs/` site (see `CLAUDE.md`).

Planned for here once there's real API surface: an OpenAPI spec snapshot (generated by `springdoc-openapi` from the running app's `/v3/api-docs.yaml`), a module dependency diagram, and an ER diagram. Empty for now — the current API surface is just the scaffolding-verification placeholder in `StatusController`.
